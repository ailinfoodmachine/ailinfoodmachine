# 产品图文详情

每个产品建立一个独立文件夹，文件夹名建议与 `data/products.json` 里的产品 `id` 保持一致。

示例：

```text
content/product-details/hydraulic-deep-drawing-press-500t/
  description.md
  parameters.xlsx
  process-flow.jpg
  application-case.jpg
```

`description.md` 可放产品介绍、应用范围、优势、配置说明、售后说明等。
